<h1>Editing tweet</h1>

<%= turbo_frame_tag dom_id(@tweet) do %>
  <div class="card">
    <%= render "form", tweet: @tweet %>
    <%# render show -> _tweet and there is a turbo_frame_tag in it %>
    <%= link_to "Cancel", tweet_path(@tweet) %>
  </div>
<% end %>

<br>

<div>
  <%= link_to "Show this tweet", @tweet %> |
  <%= link_to "Back to tweets", tweets_path %>
</div>
